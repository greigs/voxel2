{
  "name": "voxel-paint-by-numbers",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Voxel Paint-by-Numbers: client-side tool that turns voxel models into numbered, color-coded skin tiles for multicolor 3D prints painted by hand. Runs entirely in the browser.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "earcut": "^3.0.1",
    "jszip": "^3.10.1",
    "opentype.js": "^1.3.4",
    "polygon-clipping": "^0.15.7",
    "three": "^0.169.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
